import SwiftUI

@main
struct ConfirmationDialogApp: App {
    var body: some Scene {
        Window("ConfirmationDialog", id: "main") {
            ContentView()
        }
        .defaultSize(width: 400, height: 200)
    }
}