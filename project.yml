name: SwiftUIConfirmationDialogDemo
options:
  bundleIdPrefix: com.freewind
  deploymentTarget:
    macOS: "14.0"
settings:
  base:
    SWIFT_VERSION: "5.9"
    MACOSX_DEPLOYMENT_TARGET: "14.0"
targets:
  SwiftUIConfirmationDialogDemo:
    type: application
    platform: macOS
    productName: SwiftUIConfirmationDialogDemo
    sources:
      - Sources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.freewind.SwiftUIConfirmationDialogDemo
        INFOPLIST_FILE: Sources/Info.plist
        CODE_SIGN_IDENTITY: "-"
        CODE_SIGN_STYLE: Manual
        DEVELOPMENT_TEAM: ""
        ENABLE_HARDENED_RUNTIME: NO
schemes:
  SwiftUIConfirmationDialogDemo:
    build:
      targets:
        SwiftUIConfirmationDialogDemo: all
    run:
      config: Debug
